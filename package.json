{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "preview": "svelte-kit preview",
    "deploy": "rm -rf ./build && yarn build && cf push -f manifest.yml"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "next",
    "@sveltejs/kit": "next",
    "carbon-components-svelte": "^0.33.0",
    "carbon-preprocess-svelte": "^0.1.0",
    "svelte": "^3.38.2"
  }
}
